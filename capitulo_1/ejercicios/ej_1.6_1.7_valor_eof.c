#include <stdio.h>

int main()
{
	printf("El valor de la expresión getchar()!=EOF es %d\n", getchar()!=EOF);
        printf("El valor de la EOF es                      %d\n", EOF);

        return 0;
}
